# Ctrl+Shift+F10 is used to restart the session
# Ctrl+L is used to clear the console
ls()

# Para buscar en la documentación se hace con help o un ?
help('ls')
?ls
args('ls')

#para ver sets de datos podemos usar
data()
#estos ya están cargados y se pueden usar automáticamente
co2
